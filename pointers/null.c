#include<stdio.h>

int main()
{
	
			printf("size %d\n",sizeof(NULL)); 
			printf("size of void %d\n",sizeof(void));
			printf("%d\n", NULL);
		
			int* ptr = NULL;
			printf("dereferncing NULL %d", *ptr); 
}
